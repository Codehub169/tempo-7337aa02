# Example environment variables for the Startup Idea Analyzer backend

# Port for the backend server. The startup.sh script will set this to 9000 for production.
PORT=3001

# API Key for the AI service (e.g., Google Gemini API Key)
# Replace YOUR_API_KEY_HERE with your actual API key.
AI_API_KEY=YOUR_API_KEY_HERE

# Optional: AI Model to use (if configurable by the AI service)
# AI_MODEL_NAME=gemini-pro

# Optional: Node environment
# NODE_ENV=development

# Optional: Set to 'true' if the backend should serve frontend static files
# This is typically handled by NODE_ENV=production, but can be forced for other environments.
# SERVE_FRONTEND=false
